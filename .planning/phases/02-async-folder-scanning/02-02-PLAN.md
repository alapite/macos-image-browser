---
phase: 02-async-folder-scanning
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - AppState.swift
  - ContentView.swift
  - Tests/ImageBrowserTests/ScanningTests.swift
autonomous: true
---

<objective>
Add progress + "scanning" UI state, plus failure tracking surfaced to the UI.

Purpose: User must know scanning is in progress, see coarse progress, and understand if any files failed to load.
Output: UI shows scanning indicator + progress count; failures shown in UI (e.g., sidebar warning or toast).
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@AppState.swift
@ContentView.swift
@Tests/ImageBrowserTests/ScanningTests.swift
@.planning/phases/02-async-folder-scanning/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define and publish ScanProgress model in AppState</name>
  <files>AppState.swift</files>
  <action>
Extend AppState with scan progress state:

- Add `@Published var scanProgress: ScanProgress?` where `ScanProgress` is a small struct:
  - `discoveredCount: Int` (images found so far)
  - `failedCount: Int` (files that couldn't be read)
  - `isComplete: Bool` (final state)
- Update the async scan (from 02-01) to update `scanProgress` incrementally as results arrive:
  - When a file is discovered: increment `discoveredCount`.
  - When a file fails: increment `failedCount`.
- Reset `scanProgress` to `nil` when scan completes or is cancelled.
- Keep the existing `failedImages: Set<URL>` for detailed tracking; this new progress struct is for UI.
  </action>
  <verify>`swift build` succeeds</verify>
  <done>
`AppState` exposes `@Published var scanProgress: ScanProgress?` that updates during async scan.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add "scanning" indicator in ContentView</name>
  <files>ContentView.swift</files>
  <action>
Update ContentView to show scanning state:

- Add a small scanning indicator (e.g., spinner or progress bar) that appears when `appState.scanProgress != nil`.
- Show coarse progress text: "Scanning... 127 images found" (discoveredCount).
- If `scanProgress.failedCount > 0`, show a subtle warning: "12 files could not be read".
- The indicator should be visible in the main layout (e.g., top of sidebar, or overlay on main area).
- When scan completes (`scanProgress.isComplete` or nil), hide the indicator.

Constraints:
- Do not block interaction; user should be able to cancel by selecting a different folder.
- Keep the UI clean; prefer a small indicator than a full-screen modal.
  </action>
  <verify>`swift build` succeeds</verify>
  <done>
UI shows scanning indicator + count while scanning; shows failure count if any.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add tests for progress publishing</name>
  <files>Tests/ImageBrowserTests/ScanningTests.swift</files>
  <action>
Add test cases for progress behavior:

- Test that `scanProgress` updates as fake FileSystem yields results.
- Test that `scanProgress` becomes `nil` when scan completes.
- Test that `scanProgress` becomes `nil` when scan is cancelled.

Implementation:
- Use the slow/pausing fake FileSystem from 02-01 cancellation tests.
- Assert on `scanProgress` values at intermediate points.
  </action>
  <verify>`swift test --filter "ScanningTests"` passes</verify>
  <done>
Progress publishing is test-covered; all tests pass.
  </done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `swift test` passes (progress tests)
- [ ] `swift build` succeeds
- [ ] Manual smoke: select folder, see scanning indicator + count; if files fail, see warning
</verification>

<success_criteria>

- AppState publishes scan progress (discovered + failed counts)
- ContentView shows scanning indicator + progress + failure warning
- Progress behavior is test-covered
- No regressions in existing functionality

</success_criteria>

<output>
After completion, create `.planning/phases/02-async-folder-scanning/02-02-SUMMARY.md`
</output>
