---
phase: 01-baseline-test-harness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Package.swift
  - Tests/ImageBrowserTests/AppStateTests.swift
  - Tests/ImageBrowserTests/PreferencesTests.swift
  - Tests/ImageBrowserTests/TestSupport.swift
autonomous: true
---

<objective>
Add a SwiftPM test target and initial unit tests covering AppState invariants (sorting, navigation bounds) and preference encoding/decoding.

Purpose: Establish safety rails so later performance refactors (async scanning, background decode, caching) can proceed confidently.
Output: `swift test` runs successfully with a small, meaningful test suite.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@Package.swift
@AppState.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add SwiftPM test target + test directory skeleton</name>
  <files>Package.swift, Tests/ImageBrowserTests/TestSupport.swift</files>
  <action>
Update `Package.swift` to add a SwiftPM `testTarget` named `ImageBrowserTests` that depends on the `ImageBrowser` executable target.

Create `Tests/ImageBrowserTests/TestSupport.swift` with small test helpers only (no production code):
- helper for making deterministic `Date` values
- helper for creating `ImageFile` values with stable URLs and dates

Avoid adding third-party test libraries; use XCTest only.
  </action>
  <verify>`swift test` runs and discovers the (empty/initial) test bundle</verify>
  <done>
`swift test` no longer reports "no tests found" and the repo has a `Tests/ImageBrowserTests/` test module wired in SwiftPM.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add core AppState unit tests (sorting + navigation + preferences encoding)</name>
  <files>Tests/ImageBrowserTests/AppStateTests.swift, Tests/ImageBrowserTests/PreferencesTests.swift</files>
  <action>
Create focused XCTest cases that validate behavior without requiring UI.

Minimum coverage (keep tests small and behavior-focused):
- Sorting:
  - `.name` uses localized, case-insensitive ordering
  - `.creationDate` sorts by ascending creation date
  - `.custom` respects `customOrder` when entries exist, and leaves unknown images at the end
- Navigation bounds:
  - `navigateToNext()` wraps from last to first
  - `navigateToPrevious()` wraps from first to last
  - `navigateToIndex(_:)` ignores out-of-range indices and keeps current index unchanged
- Preferences codable:
  - `Preferences` encodes/decodes round-trip
  - decoding remains safe when `lastFolder` is missing (nil)

Test design constraints:
- Prefer unit tests that operate on `ImageFile` arrays and `Preferences` directly; avoid touching the real filesystem.
- Ensure tests isolate global state: do not rely on existing `UserDefaults` contents.
  </action>
  <verify>`swift test` passes; optionally `swift test --filter "ImageBrowserTests.AppStateTests"`</verify>
  <done>
At least one test exists for each category (sorting, navigation bounds, preferences codable) and all tests pass on a clean checkout.
  </done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `swift test` passes
- [ ] `swift build` still succeeds (tests did not break the executable target)
</verification>

<success_criteria>

- SwiftPM test target exists and runs under `swift test`
- Core `AppState` invariants have regression coverage (sorting + navigation bounds)
- `Preferences` codable behavior is validated

  </success_criteria>

<output>
After completion, create `.planning/phases/01-baseline-test-harness/01-01-SUMMARY.md`
</output>
